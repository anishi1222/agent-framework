// Copyright (c) Microsoft. All rights reserved.

/**
 * Provides protocol-neutral hosted identity, authorization, registry, strict JSON, dispatch,
 * cancellation, bounded streaming, and process-local continuation contracts.
 *
 * <p>This package owns no HTTP route, application framework, provider SDK, or durable state format.
 * Continuation tokens are opaque one-time process-local capabilities bound to authenticated
 * principal, isolation, route, run, type, and expiry.
 */
package com.microsoft.agents.hosting;
